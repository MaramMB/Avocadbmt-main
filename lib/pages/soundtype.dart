enum soundType { beae, asteabe }
class sound{
  String word, id, image,voice;
  soundType type;

  sound({
    required this.word,
    required this.id,
    required this.image,
    required this.voice,
    required this.type,
  });
}
